#include <bits/stdc++.h>
#define fast                          \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);
using namespace std;

#define int long long

bool func(H)

void solve()
{
    string s;
    cin >> s;
    int n = s.size();
    // /INPUT

    for (int i = 0; i < n; i++)
    {
        for (int j = i; j < n; j++)
    }
}

int32_t main()
{
    fast;
    int t{1};
    cin >> t;
    while (t--)
        solve();

    return 0;
}